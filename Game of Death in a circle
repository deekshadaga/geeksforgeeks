#include<bits/stdc++.h>
using namespace std;
int ans(int n,int k){
    if(n==1) return 1;
    else return(ans(n-1,k)+k-1)%n+1;
}
int main()
{
	int t;
    scanf("%d",&t);
    while(t--){
    int n,k;
    cin >> n >> k;
    cout <<(n,k) << endl;;
    }
	return 0;
}
