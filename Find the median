class Solution
{
public:
	public:
		int find_median(vector<int> v)
		{
		    sort(v.begin(),v.end());
		    int l=0,r=v.size();
		    int mid=l+(r-l)/2;
		    if(r%2==1) return(v[mid]);
		    else return((v[mid-1]+v[mid])/2);
		}
};
